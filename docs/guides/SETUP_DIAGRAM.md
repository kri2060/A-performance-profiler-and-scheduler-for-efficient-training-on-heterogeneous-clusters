# Visual Setup Guide

Quick visual reference for setting up your heterogeneous cluster.

---

## 🎯 Overview: What You're Building

```
┌─────────────────────────────────────────────────────────────┐
│                   YOUR TRAINING CLUSTER                      │
│                                                              │
│  ┌──────────────┐         ┌──────────────┐                 │
│  │   Master     │◄────────│   Worker 1   │                 │
│  │   RANK=0     │         │   RANK=1     │                 │
│  │ GPU/CPU      │         │   GPU/CPU    │                 │
│  │192.168.1.100 │         │192.168.1.101 │                 │
│  └──────┬───────┘         └──────────────┘                 │
│         │                                                    │
│         │                 ┌──────────────┐                 │
│         ├─────────────────│   Worker 2   │                 │
│         │                 │   RANK=2     │                 │
│         │                 │   GPU/CPU    │                 │
│         │                 │192.168.1.102 │                 │
│         │                 └──────────────┘                 │
│         │                                                    │
│         │                 ┌──────────────┐                 │
│         └─────────────────│   Worker 3   │                 │
│                           │   RANK=3     │                 │
│                           │   GPU/CPU    │                 │
│                           │192.168.1.103 │                 │
│                           └──────────────┘                 │
│                                                              │
│  Communication: Port 29500 (TCP)                            │
│  Backend: Gloo (CPU/mixed) or NCCL (all-GPU)               │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 Configuration Matrix

| Machine | Role | IP | RANK | GPU | Batch Size |
|---------|------|-------|------|-----|------------|
| Machine 1 | Master | 192.168.1.100 | 0 | RTX 3070 | 64 |
| Machine 2 | Worker | 192.168.1.101 | 1 | GTX 1660 | 64 |
| Machine 3 | Worker | 192.168.1.102 | 2 | CPU only | 32 |
| Machine 4 | Worker | 192.168.1.103 | 3 | CPU only | 32 |

**WORLD_SIZE = 4** (all machines)

---

## 🔄 Setup Flow Diagram

```
┌────────────────────────────────────────────────────────────────┐
│                      SETUP WORKFLOW                             │
└────────────────────────────────────────────────────────────────┘

┌─────────────┐
│ 1. Prepare  │
│   All       │  → Install Docker on all machines
│  Machines   │  → Check network connectivity
└──────┬──────┘  → Open firewall (port 29500)
       │
       ▼
┌─────────────┐
│ 2. Choose   │  → Option A: Docker Hub (fastest)
│ Deployment  │  → Option B: Build locally
│   Method    │  → Option C: Save/load image
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 3. Setup    │  ┌─────────────────────────────────┐
│   Master    │─>│ export WORLD_SIZE=4             │
│  (RANK=0)   │  │ ./docker_run_master_adaptive.sh │
└──────┬──────┘  └─────────────────────────────────┘
       │
       │  Master outputs: "Master IP: 192.168.1.100"
       │
       ▼
┌─────────────┐
│ 4. Setup    │  ┌─────────────────────────────────┐
│  Worker 1   │─>│ export MASTER_ADDR=192.168.1.100│
│  (RANK=1)   │  │ export RANK=1                   │
└──────┬──────┘  │ export WORLD_SIZE=4             │
       │         │ ./docker_run_worker_adaptive.sh │
       │         └─────────────────────────────────┘
       ▼
┌─────────────┐
│ 5. Setup    │  Same as Worker 1, but RANK=2
│  Worker 2   │
│  (RANK=2)   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 6. Setup    │  Same as Worker 1, but RANK=3
│  Worker 3   │
│  (RANK=3)   │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 7. Training │  Starts automatically when all
│   Starts!   │  workers connect to master
└─────────────┘
```

---

## 🔌 Network Topology

```
         Internet/LAN
              │
        ┌─────┴─────┐
        │  Router   │
        │ (Gateway) │
        └─────┬─────┘
              │
    ┌─────────┼─────────┬─────────┐
    │         │         │         │
┌───┴───┐ ┌──┴───┐ ┌──┴───┐ ┌──┴───┐
│Master │ │Work 1│ │Work 2│ │Work 3│
│  :100 │ │  :101│ │  :102│ │  :103│
└───────┘ └──────┘ └──────┘ └──────┘

Network: 192.168.1.0/24
Master Port: 29500 (must be open)
```

**Firewall Rules:**
```
Master: Allow incoming on port 29500
Workers: Allow outgoing to master:29500
```

---

## 🐳 Docker Container Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    DOCKER CONTAINER                      │
│  ┌────────────────────────────────────────────────────┐ │
│  │  PyTorch Distributed Process Group                 │ │
│  │                                                     │ │
│  │  ┌─────────────┐  ┌──────────────┐                │ │
│  │  │  Training   │  │  Profiler    │                │ │
│  │  │  Loop       │  │  (GPU/CPU)   │                │ │
│  │  └─────────────┘  └──────────────┘                │ │
│  │                                                     │ │
│  │  ┌─────────────┐  ┌──────────────┐                │ │
│  │  │ Load        │  │  Metrics     │                │ │
│  │  │ Balancer    │  │  Collector   │                │ │
│  │  └─────────────┘  └──────────────┘                │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  Volumes:                                               │
│  • /workspace → Project code                           │
│  • /experiments → Results & checkpoints                │
│                                                          │
│  Environment:                                            │
│  • MASTER_ADDR, RANK, WORLD_SIZE                       │
│                                                          │
│  Network:                                                │
│  • --network host (Linux) or port mapping (Windows)    │
└─────────────────────────────────────────────────────────┘
```

---

## ⚙️ Environment Variables Flow

```
Master Node (RANK=0):
┌────────────────────────┐
│ export WORLD_SIZE=4    │ ← Total machines
│ (RANK=0 automatic)     │ ← Master is always 0
│ (MASTER_ADDR auto)     │ ← Detected automatically
└────────────────────────┘
           │
           │ Shares IP: 192.168.1.100
           ▼
Worker Nodes (RANK=1,2,3):
┌────────────────────────┐
│ export MASTER_ADDR=... │ ← From master
│ export RANK=1          │ ← Unique per worker
│ export WORLD_SIZE=4    │ ← Same as master
└────────────────────────┘
```

**Critical Rules:**
1. **WORLD_SIZE** must be SAME on all machines
2. **RANK** must be UNIQUE (0, 1, 2, 3...)
3. **MASTER_ADDR** must be reachable from all workers

---

## 📊 Training Data Flow

```
┌─────────────────────────────────────────────────────────┐
│                   TRAINING ITERATION                     │
└─────────────────────────────────────────────────────────┘

1. Forward Pass (Each Rank Independent)
   ┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐
   │Rank 0  │   │Rank 1  │   │Rank 2  │   │Rank 3  │
   │Batch 64│   │Batch 64│   │Batch 32│   │Batch 32│
   └───┬────┘   └───┬────┘   └───┬────┘   └───┬────┘
       │            │            │            │
       ▼            ▼            ▼            ▼
   ┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐
   │ Model  │   │ Model  │   │ Model  │   │ Model  │
   │Forward │   │Forward │   │Forward │   │Forward │
   └───┬────┘   └───┬────┘   └───┬────┘   └───┬────┘
       │            │            │            │
       ▼            ▼            ▼            ▼
   ┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐
   │ Loss   │   │ Loss   │   │ Loss   │   │ Loss   │
   └───┬────┘   └───┬────┘   └───┬────┘   └───┬────┘
       │            │            │            │
       └────────────┴────────────┴────────────┘
                    │
2. Gradient Synchronization (AllReduce)
                    ▼
           ┌─────────────────┐
           │  Average        │
           │  Gradients      │
           │  across ranks   │
           └────────┬────────┘
                    │
       ┌────────────┼────────────┬────────────┐
       ▼            ▼            ▼            ▼
   ┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐
   │Update  │   │Update  │   │Update  │   │Update  │
   │Weights │   │Weights │   │Weights │   │Weights │
   └────────┘   └────────┘   └────────┘   └────────┘

3. Load Balancer (Optional)
   ┌─────────────────────────────────┐
   │ Measure: iteration time/rank    │
   │ Adjust: batch sizes dynamically │
   │ Goal: minimize stragglers       │
   └─────────────────────────────────┘
```

---

## 🎨 Hardware Detection Flow

```
Container Starts
      │
      ▼
┌─────────────────┐
│ Check nvidia-smi│
└────────┬────────┘
         │
    ┌────┴────┐
    │ GPU?    │
    └────┬────┘
         │
    ┌────┴─────────────────────┐
    │                          │
    ▼ YES                      ▼ NO
┌─────────┐              ┌──────────┐
│GPU Mode │              │CPU Mode  │
│         │              │          │
│• CUDA   │              │• CPU     │
│• Batch64│              │• Batch32 │
│• NCCL/  │              │• Gloo    │
│  Gloo   │              │          │
└────┬────┘              └────┬─────┘
     │                        │
     └────────┬───────────────┘
              ▼
     ┌────────────────┐
     │ Start Training │
     └────────────────┘
```

**Automatic Features:**
- ✅ GPU/CPU detection
- ✅ Batch size adjustment
- ✅ Backend selection
- ✅ Memory optimization

---

## 🚦 Launch Sequence Timeline

```
Time    Master                  Worker 1              Worker 2              Worker 3
─────────────────────────────────────────────────────────────────────────────────────
0s      Start master
        Build/pull image

5s      Master ready
        Listening on :29500
        "Waiting 60s..."

10s                             Start worker 1
                                Pull image

15s                             Connect to master     Start worker 2
                                                      Pull image

20s                             Connected ✓           Connect to master    Start worker 3
                                                                           Pull image

25s                                                   Connected ✓          Connect to master

30s                                                                        Connected ✓

35s     All workers connected!
        Initialize training

40s     Start Epoch 1          Start Epoch 1         Start Epoch 1        Start Epoch 1

45s     Training...            Training...           Training...          Training...
        ▓▓▓▓▓▓▓▓▓▓            ▓▓▓▓▓▓▓▓              ▓▓▓                  ▓▓▓
        (fast GPU)             (medium GPU)          (slow CPU)           (slow CPU)
```

**Key Points:**
- Master starts first (waits 60s)
- Workers connect within 60s window
- Training begins when all connect
- Different speeds are normal (heterogeneous!)

---

## 📈 Performance Expectations

```
Single GPU (baseline):         ████████████████████  100% (256 samples/s)

2 GPUs (homogeneous):          ████████████████████  200% (512 samples/s)
                               ████████████████████

2 GPUs (heterogeneous):        ████████████████████  180% (460 samples/s)
RTX 3070                       ████████████████████  (faster)
GTX 1660                       ████████████          (slower)

2 GPUs + 2 CPUs (mixed):       ████████████████████  220% (563 samples/s)
RTX 3070                       ████████████████████
GTX 1660                       ████████████
CPU 1                          ███
CPU 2                          ██

With Load Balancing:           ████████████████████  240% (614 samples/s)
RTX 3070 (optimized)           ████████████████████
GTX 1660 (optimized)           ███████████████
CPU 1 (optimized)              ████
CPU 2 (optimized)              ████
```

**Benefits:**
- Utilize all available hardware
- 20-40% speedup with load balancing
- Scale linearly with more machines

---

## 🔍 Verification Checklist

```
Before Starting:
┌─────────────────────────────────────────────┐
│ ☐ Docker installed on all machines         │
│ ☐ Machines can ping each other             │
│ ☐ Port 29500 open on master                │
│ ☐ Same WORLD_SIZE on all machines          │
│ ☐ Unique RANK for each worker              │
│ ☐ Master IP noted and shared               │
└─────────────────────────────────────────────┘

After Starting:
┌─────────────────────────────────────────────┐
│ ☐ Master container running                  │
│ ☐ All worker containers running             │
│ ☐ No connection errors in logs              │
│ ☐ Training started (check logs)             │
│ ☐ Loss decreasing each epoch                │
└─────────────────────────────────────────────┘
```

---

## 🎯 Quick Command Reference

### Master:
```bash
export WORLD_SIZE=4
./scripts/docker_run_master_adaptive.sh
```

### Worker (Linux/Mac):
```bash
export MASTER_ADDR=192.168.1.100
export RANK=1  # Unique!
export WORLD_SIZE=4
./scripts/docker_run_worker_adaptive.sh
```

### Worker (Windows):
```cmd
set MASTER_ADDR=192.168.1.100
set RANK=1
set WORLD_SIZE=4
scripts\docker_run_worker_adaptive.bat
```

### Monitor:
```bash
docker logs -f hetero-master
docker ps
./scripts/docker_run_dashboard.sh
```

---

## 🎓 Next Steps

1. ✅ Understand the architecture (this document)
2. 📋 Complete prerequisites ([SETUP_CHECKLIST.md](SETUP_CHECKLIST.md))
3. 🚀 Follow quick start ([QUICK_START_MULTINODE.md](QUICK_START_MULTINODE.md))
4. 📊 Launch training ([LAUNCH_GUIDE.md](LAUNCH_GUIDE.md))
5. 🎉 Enjoy distributed training!

---

**Ready to build your cluster? Start here:** [QUICK_START_MULTINODE.md](QUICK_START_MULTINODE.md) 🚀
